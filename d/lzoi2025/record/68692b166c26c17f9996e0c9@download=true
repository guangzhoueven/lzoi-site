#include <iostream>
using namespace std;
int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);
	int n;
	cin >> n;
	int days = 0, num = 0;
	while (n > 0) {
		days++;
		if (num == 0 && n % 3 == 1) num = days;
		n = n * 2 / 3;
	}
	cout << days << " " << num << "\n";
	return 0;
}